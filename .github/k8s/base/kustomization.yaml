apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - collector-config.yaml
  - collector-deployment.yaml
  - collector-service.yaml
  - collector-ingress.yaml
  - migrator-job.yaml

commonLabels:
  app.kubernetes.io/part-of: sophonz-otel
